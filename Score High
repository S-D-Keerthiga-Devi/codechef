#include <iostream>
#include<algorithm>
using namespace std;

int main() {
	// your code goes here
	int t,a,b,c,d,x,n,y,z;
	cin>>t;
	while(t>0)
	{
	    cin>>n;
	    cin>>a>>b>>c>>d;
	    x=max(a,b);
	    y=max(x,c);
	    z=max(y,d);
	    cout<<z<<endl;
	    t--;
	}
	return 0;
}
