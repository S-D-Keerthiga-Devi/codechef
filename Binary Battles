#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t,n,a,b,x,y;
	cin>>t;
	while(t>0)
	{
	    cin>>n>>a>>b;
	    x=log(n)/log(2);
	    y=x-1;
	    cout<<((x*a)+(y*b))<<endl;
	    t--;
	    x=0;
	    y=0;
	}
	return 0;
}
